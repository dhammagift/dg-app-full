# App Store скриншоты (iOS)

Реальные снимки симулятора (`test/ios-sim/drive.sh --tour`, прогон 156, сборка `4d0f2cc`),
эталонный набор без повторов — тема light, локаль en.

## iphone-6.7in-1284x2778/

1284×2778 — принимается в слот **iPhone 6.5" Display** в App Store Connect (наряду с
1242×2688). Порядок и состав — по `docs/APP_STORE_LISTING.md`:

1. `01-search.png` — поиск с подсветкой
2. `02-reader.png` — текст с переводом
3. `03-dictionary.png` — словарь
4. `04-favorites-history.png` — избранное/история
5. `05-settings.png` — настройки
6. `06-home.png` — стартовый экран

Показываются в листинге первые 3 — их порядок продаёт приложение, менять с осторожностью.

Другие темы/локали (dark-en, dark-ru) и набор 6.9" (`1320×2868`, неполный: нет search/reader/
favorites-history/settings в light-en) — в `docs/ios-review/tour-156/`.

## iPad

Пока нет: планшетный проход тура на момент прогона 156 не отработал (см.
`docs/ios-review/README.md`), а более новый артефакт с ним (`dg-app-ios-screenshots-174`,
прогон 174, сегодня) недоступен для скачивания в этой сессии — сетевая политика блокирует
`productionresultssa19.blob.core.windows.net` (хранилище артефактов GitHub Actions), это не
обходится. Нужен один из вариантов:
- скачать `dg-app-ios-screenshots-174` вручную (GitHub → Actions → run 174 → Artifacts) и
  положить `iPad Pro 13"` кадры сюда;
- либо открыть доступ прокси к этому хосту и повторить запрос в новой сессии.

## App Previews (видео)

Не готовы — нужна запись экрана из симулятора/устройства, в текущих прогонах CI это не
захватывается.
