# App Store скриншоты и превью (iOS)

`screenshots/` и `previews/` — материалы для страницы приложения в App Store Connect. Реальные
снимки/видео симулятора (`test/ios-sim/drive.sh --tour`), не макеты.

## screenshots/ — пусто, снимки удалены (20.09, ночь)

Набор из прогона 180 (`3c1c359`) оказался непригоден: владелец проверил картинки вручную —
`03-dictionary.png` показывает не словарь ("что-то другое", возможно вообще Android), то есть тур
кликал не туда или снимал не тот момент. Прежде чем публиковать следующий набор — **открыть
каждую картинку и проверить глазами**, что на ней реально нужный экран, а не только что имя файла
совпадает со стадией тура. Код тура (`test/ios-sim/tour.js`, `test/ios-sim/drive.sh`,
`test/ios-sim/make-previews.js`) не удалён и может быть рабочим, но его вывод не был перепроверен
перед прошлым коммитом.

`home`-стадия тура и раньше не работала (показывала поиск kacchapa вместо hero) — см. журнал
коммитов этой ветки, если понадобится история проблемы.

## App Previews (видео) — `previews/iphone-6.7in-1284x2778/`

Не готовы. Джоба `ios-screenshots` записывает видео (`drive.sh --record`) и режет его на 3 клипа
(`test/ios-sim/make-previews.js`), но во всех трёх прогонах, где нарезка запускалась (177, 179,
180), результат — 0 файлов; шаг «Cut the 3 App Preview clips» завершается без ошибки, но клипы не
появляются, причина не диагностирована (нужен доступ к логам/файлам самого прогона, которого в
этой сессии нет — см. ниже).

## Почему тут появляется CI-коммит, а не только артефакт

Артефакт джобы (`dg-app-ios-screenshots-<run>`) лежит на `productionresultssa19.blob.core.windows.net`
— это не `github.com`, и в этой сессии такой хост заблокирован сетевой политикой (см. run 174,
скачать не удалось). Поэтому джоба сама коммитит скриншоты и превью в эту ветку последним шагом
(`git push`, `[skip ci]`, чтобы не зациклить сборку) — их можно забрать обычным `git pull`, без
похода за артефактом.
