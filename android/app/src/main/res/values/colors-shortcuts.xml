<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- dg-twa's drawable-anydpi/shortcut_legacy_background.xml (copied with the shortcut icons)
         paints this colour behind the icon on launchers that predate adaptive icons. The colour
         lives in dg-twa's own values/colors.xml, generated by bubblewrap, so it has to be carried
         over with the drawable — AAPT fails the whole build without it (learned the hard way). -->
    <color name="shortcut_background">#F5F5F5</color>
</resources>
