<?xml version="1.0" encoding="utf-8"?>
<!-- Static App Shortcuts (long-press the launcher icon).

     Icons come from dg-twa (owner: "шорткаты и иконки неактуальные возьми из dg-twa там последняя
     версия") — drawable-*/shortcut_N.png, mirrored here.

     ONE static shortcut, deliberately. The launcher's long-press menu holds four entries and it
     counts what is DECLARED HERE against that budget even when a declared shortcut is disabled at
     runtime — which is what cost the third "recently read" slot (owner: "должны быть 3 пункта под
     историю... почему-то всё ещё два"). The dictionary app was built the other way round first and
     showed exactly the same two out of three, so both apps now keep one declared entry and push
     everything else:

       "Recent texts in app shortcuts" OFF -> three programmed (Contents, Memo, Dictionary) as
                                              dynamic shortcuts, native-bridge.js, each with the
                                              drawable it had while it was static;
       ON (default)                       -> up to three "recently read";
       either way                         -> plus this one, which is the errand a reader returns to
                                              and the only entry that exists before the app has ever
                                              run (a dynamic shortcut cannot).

     It has to be STATIC — the first attempt kept Contents/Favorites as dynamic shortcuts with
     rank 0/1, on the assumption that Android lists dynamic ones above static ones. On the owner's
     launcher the opposite happens: the static pair (Dictionary, Memo) came first and the pinned
     two fell to the bottom, which is exactly the screenshot that reopened this.

     Each shortcut launches MainActivity with a "route" extra; MainActivity turns it into
     https://localhost/?_nativeRoute=..., and the page rewrites the visible URL before its own
     bootstrap reads it. Two of the three programmed routes are routed to the real browser by
     native-bridge.js (see EXTERNAL_ROUTES there): /dict is a server-rendered page of
     dict.dhamma.gift, /memo is deliberately not bundled. -->
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="favorites_history"
        android:enabled="true"
        android:icon="@drawable/shortcut_0"
        android:shortcutShortLabel="@string/shortcut_fav_short"
        android:shortcutLongLabel="@string/shortcut_fav_long">
        <intent
            android:action="android.intent.action.MAIN"
            android:targetPackage="gift.dhamma.mobile"
            android:targetClass="gift.dhamma.mobile.MainActivity">
            <extra android:name="route" android:value="/4as" />
        </intent>
    </shortcut>
</shortcuts>
